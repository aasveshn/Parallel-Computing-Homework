#include <iostream>
#include <thread> 
#include <vector>
#include <chrono>

#include "FirstTask/FirstTask.h"

int main()
{
  FirstTask(1, 20);
  FirstTask(2, 20);
  FirstTask(3, 20);
  FirstTask(4, 20);
  return 0;
}
